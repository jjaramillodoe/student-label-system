'use client';

/**
 * @deprecated Horizontal admin toolbar replaced by AppShell / AppSidebar.
 * Kept as a no-op so any leftover imports do not break the build.
 */
export default function AdminHeader() {
  return null;
}
