---
title: QR codes
description: Public URLs for student labels and archive boxes.
---

## Student label QR

```
https://student-label-system.vercel.app/student/{labelId}
```

Public page shows Label ID, Student ID, name, DOB, cabinet/drawer **or** archive box location, school, and sibling links.

<Frame caption="Public student record opened from a label QR (`/student/{labelId}`)">
  <img src="/images/screenshots/public-student-page.png" alt="Public student record for an archived student with archive box and storage location" />
</Frame>

## Archive box QR

```
https://student-label-system.vercel.app/archive/box/{boxId}
```

Public page shows box label, cabinet, school year, storage location, archive date, and the full student file list. Supports print and PDF download.

Intake uses the same box URL (with QR) when staff log a visit for an **archived** returning student so they can find paperwork without a new drawer.

<Frame caption="Placeholder — public archive box page (`/archive/box/{boxId}`)">
  <img src="/images/placeholders/public-archive-box.svg" alt="Placeholder: Public archive box page" />
</Frame>

## Mintlify search MCP (this docs site)

After you deploy this documentation on Mintlify, your hosted MCP URL will be:

```
https://YOUR-SUBDOMAIN.mintlify.app/mcp
```

Replace `YOUR-SUBDOMAIN` with your Mintlify subdomain from the dashboard. Use that URL in Cursor so AI can search **your** documentation while you write or support staff.

For **district79**, use `https://district79.mintlify.app/mcp`.
