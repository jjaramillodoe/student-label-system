---
title: Duplicates and siblings
description: Review flagged and auto-detected duplicate student records with address comparison.
---

Data Leads use **Students → Duplicates** (`/admin/duplicates`) to resolve potential duplicate records. The page has two tabs:

1. **Live students** — sibling / duplicate pairs already in the system (merge, confirm, dismiss)
2. **Legacy MDB import** — scan the school’s uploaded ASISTS/MDB roster for garbage data and matches against live students

## Who can access

**Admin** and **Data Lead** only.

## How intake surfaces matches

During **NEW** registration, Intake checks in two stages:

1. **Check ASISTS first** — search by name, DOB (MM/DD/YYYY or YYYY-MM-DD), or both. A DOB-only search returns only students born that day.
2. **After unlock** — when personal info is filled, the system re-checks **same DOB** (always shown for sibling review), **similar name** (match %), and **home address**.

Staff can:

- Use **Same person sitting here — log returning** when the match is the student at the desk
- Check **Not the same person sitting here** (ASISTS gate) or **This is a different person with the same name** (duplicate panel) to flag siblings for Data Lead review
- Notify the Data Lead with **Copy alert message** (Teams / Slack / paste into email) or **Email with alert** (opens mail with subject + body filled in)

Each match card shows a **% match** (name similarity) and a **Same DOB** badge when birthdays align.

## Intake alert message

The copied alert is written for Data Lead review. It includes:

- A clear **Subject** line (`Possible duplicate student — Intake alert`)
- School, who reported it, and when
- The NEW student being registered (name, DOB, phone, email, address — shows **—** if a field is still blank)
- Possible matches in this system and on ASISTS / legacy (DOB, Label ID, status, similarity, same-DOB / address notes)
- Short **What to do** steps and a link to `/admin/duplicates`

If intake already checked **different person**, the intro line says the registration was flagged as a sibling / coincidence path.

<Note>
  **ASISTS gate:** after a name or DOB search, confirm **Same person sitting here — log returning**, or check **Not the same person sitting here** to unlock NEW intake (flagged for Data Lead review).
</Note>

<Frame caption="Intake duplicate alert — match scores, Same DOB, and sibling acknowledgement">
  <img src="/images/screenshots/intake-duplicates.png" alt="Intake form showing possible duplicate matches with percent match badges and sibling flag checkbox" />
</Frame>

## Flagged section

Students marked by Intake Members with **This is a different person** appear here for review.

Intake may flag a pair when names and DOB are similar but the student insists they are a different person — often siblings.

## Auto-detected section

The system scans students for:

- **Same DOB + similar name** (fuzzy match, including middle-name omissions), **or**
- **Same DOB + same home address** (even when names do not fuzzy-match)

On Intake, **all same-DOB** students also appear for review (with match %), so siblings with different first names are not dropped when staff type a new name.

Pairs already confirmed as siblings, manually dismissed, or still flagged in the first section are excluded.

## Address comparison

Between each pair, a comparison block shows:

| Badge | Meaning |
| --- | --- |
| **Same verified address** | NYC-standardized data matches |
| **Same address** | Street, apt, city, and ZIP align |
| **Same building** | Same building, different unit — likely siblings |
| **Different address** | Different homes — sibling at new address or possible move |

<Tip>
  **Matched by address** appears when the pair was surfaced primarily by home address, not name similarity.
</Tip>

Each student card also shows their formatted address (street + apt on one line, city/state/ZIP below) and a **Verified** badge when NYC Geoclient confirmed the record.

## Actions

| Action | Result |
| --- | --- |
| **Confirm Siblings** | Links both records bidirectionally; sibling badge appears on student pages |
| **Merge Records** | Choose which record to keep, then pick which fields to keep (email, phone, program, notes, address group, etc.); secondary is deleted; **Undo** ~60 seconds (snackbar) or ~15 minutes from **Recent merges** |
| **Dismiss / Not a duplicate** | Hides the pair permanently |
| **Confirm/Dismiss all same-building** | Bulk action for pairs marked **Same building** (likely siblings in different units) |

### Merge field review

When you click **Merge Records**:

1. Select the **primary** student (the record that remains) — each card shows a **completeness** count (filled contact/address fields)
2. Read any **warnings** (e.g. secondary name looks more complete, DOB differs)
3. Review the **field diff** — each row shows Same / Conflict / Only primary / Only secondary
4. Click a value to keep it (defaults: primary wins on conflicts; secondary fills empties)
5. **Home address** is chosen as one group (street, apt, city, ZIP + verification)
6. If primary is unassigned and secondary has a drawer, optionally **transfer drawer location** to the primary (default on when available)
7. Confirm — secondary is deleted; drawer is transferred or freed depending on that option

Use **All primary** or **Prefer secondary fills** for quick defaults, then adjust conflicts after verifying with the student.

<Warning>
  **Merge** deletes the non-primary record (drawer is transferred or freed). A snackbar offers **Undo** for about 60 seconds; **Recent merges** keeps undo for about 15 minutes. After that, restore needs Admin help. Verify conflicting fields before confirming.
</Warning>

## Recent merges

The bottom of the **Live students** tab lists recent merges for your school (who merged, which names, field change count). Use **Undo** there while the 15-minute window remains.

## Legacy MDB import review

Upload the school’s ASISTS Access (`.mdb` / `.accdb`) or CSV roster under **Admin → Schools** (legacy roster upload). Then open **Duplicates → Legacy MDB import**.

The review scans `school_legacy_roster` for that school and reports:

| Bucket | Meaning |
| --- | --- |
| **Garbage / low quality** | 1-letter names, placeholders (TBD/N/A), missing/invalid/future DOB, numeric names, etc. |
| **ID conflicts** | Same ASISTS/external ID as a live student, but the **name differs** |
| **Fuzzy matches** | Likely already filed (name/DOB similarity) — review before creating another record |
| **Exact in system** | Same name+DOB or same ASISTS ID already registered |
| **Dupes in MDB** | Identical rows repeated inside the import file |
| **Legacy only** | In the MDB but not yet in this system (sample) |

This tab is **read-only** — it does not merge or delete. Use Intake / live Duplicate Review for filing actions. Re-upload the MDB from School Settings when ASISTS data changes.

## Related pages

- [Intake workflow](/workflows/intake) — how intake flags siblings and runs address-aware duplicate checks
- [Address and NYC verification](/reference/address-and-verification)

<Frame caption="Students → Duplicates — review flagged siblings and auto-detected pairs">
  <img src="/images/screenshots/duplicates-page.png" alt="Duplicate Review page with confirm, merge, and dismiss guidance" />
</Frame>
